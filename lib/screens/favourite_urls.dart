import 'package:flutter/material.dart';

class FavouriteUrls extends StatelessWidget {
  const FavouriteUrls({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: const Text('Favourite Urls'),
        ),
      );
}
